[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "image-captioning"
version = "0.1.0"
description = "Object-oriented image captioning pipelines"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]